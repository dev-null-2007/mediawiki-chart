{{- /*
Copyright Broadcom, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and (include "mediawiki.host" .) (or .Values.mariadb.enabled .Values.externalDatabase.host) -}}
apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
kind: Deployment
metadata:
  name: {{ template "common.names.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: mediawiki
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.replicaCount }}
  {{- $podLabels := include "common.tplvalues.merge" ( dict "values" ( list .Values.podLabels .Values.commonLabels ) "context" . ) }}
  selector:
    matchLabels: {{- include "common.labels.matchLabels" ( dict "customLabels" $podLabels "context" $ ) | nindent 6 }}
      app.kubernetes.io/component: mediawiki
  strategy: {{- include "common.tplvalues.render" (dict "value" .Values.updateStrategy "context" $ ) | nindent 4 }}
  template:
    metadata:
      labels: {{- include "common.labels.standard" ( dict "customLabels" $podLabels "context" $ ) | nindent 8 }}
        app.kubernetes.io/component: mediawiki
      {{- if or .Values.podAnnotations (and .Values.metrics.enabled .Values.metrics.podAnnotations) }}
      annotations:
        {{- if .Values.podAnnotations }}
        {{- include "common.tplvalues.render" (dict "value" .Values.podAnnotations "context" $) | nindent 8 }}
        {{- end }}
        {{- if and .Values.metrics.enabled .Values.metrics.podAnnotations }}
        {{- include "common.tplvalues.render" (dict "value" .Values.metrics.podAnnotations "context" $) | nindent 8 }}
        {{- end }}
      {{- end }}
    spec:
      {{- include "mediawiki.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "mediawiki.serviceAccountName" . }}
      automountServiceAccountToken: {{ .Values.automountServiceAccountToken }}
      {{- if .Values.hostAliases }}
      # yamllint disable rule:indentation
      hostAliases: {{- include "common.tplvalues.render" (dict "value" .Values.hostAliases "context" $) | nindent 8 }}
      # yamllint enable rule:indentation
      {{- end }}
      {{- if .Values.affinity }}
      affinity: {{- include "common.tplvalues.render" ( dict "value" .Values.affinity "context" $) | nindent 8 }}
      {{- else }}
      affinity:
        podAffinity: {{- include "common.affinities.pods" (dict "type" .Values.podAffinityPreset "customLabels" $podLabels "context" $) | nindent 10 }}
        podAntiAffinity: {{- include "common.affinities.pods" (dict "type" .Values.podAntiAffinityPreset "customLabels" $podLabels "context" $) | nindent 10 }}
        nodeAffinity: {{- include "common.affinities.nodes" (dict "type" .Values.nodeAffinityPreset.type "key" .Values.nodeAffinityPreset.key "values" .Values.nodeAffinityPreset.values) | nindent 10 }}
      {{- end }}
      {{- if .Values.schedulerName }}
      schedulerName: {{ .Values.schedulerName }}
      {{- end }}
      {{- if .Values.topologySpreadConstraints }}
      topologySpreadConstraints: {{- include "common.tplvalues.render" (dict "value" .Values.topologySpreadConstraints "context" .) | nindent 8 }}
      {{- end }}
      priorityClassName: {{ .Values.priorityClassName | quote }}
      {{- if .Values.nodeSelector }}
      nodeSelector: {{- include "common.tplvalues.render" ( dict "value" .Values.nodeSelector "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.tolerations }}
      tolerations: {{- include "common.tplvalues.render" (dict "value" .Values.tolerations "context" .) | nindent 8 }}
      {{- end }}
      {{- if .Values.podSecurityContext.enabled }}
      securityContext: {{- include "common.compatibility.renderSecurityContext" (dict "secContext" .Values.podSecurityContext "context" $) | nindent 8 }}
      {{- end }}
      initContainers:
        - name: prepare-base-dir
          image: {{ include "mediawiki.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy | quote }}
          {{- if .Values.resources }}
          resources: {{- toYaml .Values.resources | nindent 12 }}
          {{- else if ne .Values.resourcesPreset "none" }}
          resources: {{- include "common.resources.preset" (dict "type" .Values.resourcesPreset) | nindent 12 }}
          {{- end }}
          {{- if .Values.containerSecurityContext.enabled }}
          securityContext: {{- include "common.compatibility.renderSecurityContext" (dict "secContext" .Values.containerSecurityContext "context" $) | nindent 12 }}
          {{- end }}
          env:
            - name: MEDIAWIKI_DB_HOST
              value: {{ include "mediawiki.databaseHost" . | quote }}
            - name: MEDIAWIKI_DB_PORT
              value: {{ include "mediawiki.databasePort" . | quote }}
            - name: MEDIAWIKI_DB_NAME
              value: {{ include "mediawiki.databaseName" . | quote }}
            - name: MEDIAWIKI_DB_USER
              value: {{ include "mediawiki.databaseUser" . | quote }}
            - name: MEDIAWIKI_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "mediawiki.databaseSecretName" . }}
                  key: mariadb-password
            {{- $port:= ( coalesce .Values.service.ports.http .Values.service.port) | toString }}
            - name: MEDIAWIKI_SITE_SERVER
              value: "http://{{ include "mediawiki.host" . }}{{- if ne $port "80" }}:{{ coalesce .Values.service.ports.http .Values.service.port }}{{ end }}"
            - name: MEDIAWIKI_SITE_NAME
              value: {{ .Values.mediawikiName | quote }}
            - name: MEDIAWIKI_ADMIN_USER
              value: {{ .Values.mediawikiUser | quote }}
            - name: MEDIAWIKI_ADMIN_PASS
              valueFrom:
                secretKeyRef:
                  name: {{ include "mediawiki.secretName" . }}
                  key: mediawiki-password
          command:
            - /bin/bash
          args:
            - -ec
            - |
              #!/bin/bash
              echo "Preparing directories for MediaWiki..."
              # Ensure required directories exist with proper permissions
              mkdir -p /emptydir/apache-conf-dir /emptydir/apache-logs-dir /emptydir/apache-tmp-dir
              mkdir -p /emptydir/php-conf-dir /emptydir/php-tmp-dir /emptydir/php-var-dir
              mkdir -p /emptydir/tmp-dir

              # Copy PHP extension configs if not present (critical for intl and other extensions)
              if [ ! -f /emptydir/php-conf-dir/docker-php-ext-intl.ini ]; then
                echo "Copying PHP extension configuration files..."
                cp -a /usr/local/etc/php/conf.d/* /emptydir/php-conf-dir/ 2>/dev/null || true
                echo "PHP configuration files copied"
              fi

              # Wait for persistent volume to be ready (avoid timing issues)
              echo "Waiting for persistent volume to be ready..."
              timeout=30
              while [ $timeout -gt 0 ]; do
                if touch /mediawiki-data/.volume-test 2>/dev/null; then
                  rm -f /mediawiki-data/.volume-test
                  echo "Persistent volume is ready"
                  break
                fi
                echo "Volume not ready, waiting... ($timeout seconds remaining)"
                sleep 1
                timeout=$((timeout-1))
              done

              if [ $timeout -eq 0 ]; then
                echo "ERROR: Persistent volume failed to become ready"
                exit 1
              fi

              # Copy MediaWiki files to persistent volume if not already present
              if [ ! -f /mediawiki-data/index.php ]; then
                echo "Copying MediaWiki installation to persistent volume..."
                cp -a /var/www/html/. /mediawiki-data/
                echo "MediaWiki files copied successfully"
              else
                echo "MediaWiki files already present in persistent volume"
              fi

              # Generate LocalSettings.php if not present
              if [ ! -f /mediawiki-data/LocalSettings.php ]; then
                echo "Generating LocalSettings.php..."
                cd /mediawiki-data
                php maintenance/install.php \
                  --dbtype=mysql \
                  --dbserver="${MEDIAWIKI_DB_HOST}:${MEDIAWIKI_DB_PORT}" \
                  --dbname="${MEDIAWIKI_DB_NAME}" \
                  --dbuser="${MEDIAWIKI_DB_USER}" \
                  --dbpass="${MEDIAWIKI_DB_PASSWORD}" \
                  --server="${MEDIAWIKI_SITE_SERVER}" \
                  --scriptpath="" \
                  --pass="${MEDIAWIKI_ADMIN_PASS}" \
                  "${MEDIAWIKI_SITE_NAME}" \
                  "${MEDIAWIKI_ADMIN_USER}"

                # Append include statement for custom settings
                echo "" >> LocalSettings.php
                echo "# Include custom configuration" >> LocalSettings.php
                echo "require_once '/config/LocalSettings.custom.php';" >> LocalSettings.php

                echo "LocalSettings.php generated successfully"
              else
                echo "LocalSettings.php already exists"
                # Check if custom settings include is present, add if missing
                if ! grep -q "LocalSettings.custom.php" /mediawiki-data/LocalSettings.php; then
                  echo "Adding custom settings include to existing LocalSettings.php..."
                  echo "" >> /mediawiki-data/LocalSettings.php
                  echo "# Include custom configuration" >> /mediawiki-data/LocalSettings.php
                  echo "require_once '/config/LocalSettings.custom.php';" >> /mediawiki-data/LocalSettings.php
                fi
              fi

              echo "Directory preparation completed"
          volumeMounts:
            - name: empty-dir
              mountPath: /emptydir
            - name: mediawiki-data
              mountPath: /mediawiki-data
              subPath: mediawiki
            - name: custom-settings
              mountPath: /config
        {{- if .Values.initContainers }}
        {{- include "common.tplvalues.render" (dict "value" .Values.initContainers "context" $) | nindent 8 }}
        {{- end }}
      containers:
        - name: {{ template "common.names.fullname" . }}
          image: {{ template "mediawiki.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy | quote }}
          {{- if .Values.containerSecurityContext.enabled }}
          securityContext: {{- include "common.compatibility.renderSecurityContext" (dict "secContext" .Values.containerSecurityContext "context" $) | nindent 12 }}
          {{- end }}
          {{- if .Values.command }}
          command: {{- include "common.tplvalues.render" (dict "value" .Values.command "context" $) | nindent 12 }}
          {{- end }}
          {{- if .Values.args }}
          args: {{- include "common.tplvalues.render" (dict "value" .Values.args "context" $) | nindent 12 }}
          {{- end }}
          {{- if .Values.lifecycleHooks }}
          lifecycle: {{- include "common.tplvalues.render" (dict "value" .Values.lifecycleHooks "context" $) | nindent 12 }}
          {{- end }}
          env:
            - name: MEDIAWIKI_DB_TYPE
              value: "mysql"
            - name: MEDIAWIKI_DB_HOST
              value: {{ include "mediawiki.databaseHost" . | quote }}
            - name: MEDIAWIKI_DB_PORT
              value: {{ include "mediawiki.databasePort" . | quote }}
            - name: MEDIAWIKI_DB_NAME
              value: {{ include "mediawiki.databaseName" . | quote }}
            - name: MEDIAWIKI_DB_USER
              value: {{ include "mediawiki.databaseUser" . | quote }}
            - name: MEDIAWIKI_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "mediawiki.databaseSecretName" . }}
                  key: mariadb-password
            {{- $port:= ( coalesce .Values.service.ports.http .Values.service.port) | toString }}
            - name: MEDIAWIKI_SITE_SERVER
              value: "http://{{ include "mediawiki.host" . }}{{- if ne $port "80" }}:{{ coalesce .Values.service.ports.http .Values.service.port }}{{ end }}"
            - name: MEDIAWIKI_SITE_NAME
              value: {{ .Values.mediawikiName | quote }}
            - name: MEDIAWIKI_ADMIN_USER
              value: {{ .Values.mediawikiUser | quote }}
            - name: MEDIAWIKI_ADMIN_PASS
              valueFrom:
                secretKeyRef:
                  name: {{ include "mediawiki.secretName" . }}
                  key: mediawiki-password
            {{- if .Values.mediawikiEmail }}
            - name: MEDIAWIKI_EMAIL
              value: {{ .Values.mediawikiEmail | quote }}
            {{- end }}
            {{- if .Values.smtpHostID }}
            - name: SMTP_HOST_ID
              value: {{ .Values.smtpHostID | quote }}
            {{- end }}
            {{- if .Values.smtpHost }}
            - name: SMTP_HOST
              value: {{ .Values.smtpHost | quote }}
            {{- end }}
            {{- if .Values.smtpPort }}
            - name: SMTP_PORT
              value: {{ .Values.smtpPort | quote }}
            {{- end }}
            {{- if .Values.smtpUser }}
            - name: SMTP_USER
              value: {{ .Values.smtpUser | quote }}
            {{- end }}
            {{- if .Values.smtpPassword }}
            - name: SMTP_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "mediawiki.secretName" . }}
                  key: smtp-password
            {{- end }}
            {{- if .Values.extraEnvVars }}
            {{- include "common.tplvalues.render" (dict "value" .Values.extraEnvVars "context" $) | nindent 12 }}
            {{- end }}
          {{- if or .Values.extraEnvVarsCM .Values.extraEnvVarsSecret }}
          envFrom:
            {{- if .Values.extraEnvVarsCM }}
            - configMapRef:
                name: {{ include "common.tplvalues.render" (dict "value" .Values.extraEnvVarsCM "context" $) }}
            {{- end }}
            {{- if .Values.extraEnvVarsSecret }}
            - secretRef:
                name: {{ include "common.tplvalues.render" (dict "value" .Values.extraEnvVarsSecret "context" $) }}
            {{- end }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.containerPorts.http }}
            - name: https
              containerPort: {{ .Values.containerPorts.https }}
            {{- if .Values.extraContainerPorts }}
            {{- include "common.tplvalues.render" (dict "value" .Values.extraContainerPorts "context" $) | nindent 12 }}
            {{- end }}
          {{- if .Values.customStartupProbe }}
          startupProbe: {{- include "common.tplvalues.render" (dict "value" .Values.customStartupProbe "context" $) | nindent 12 }}
          {{- else if .Values.startupProbe.enabled }}
          startupProbe: {{- include "common.tplvalues.render" (dict "value" (omit .Values.startupProbe "enabled") "context" $) | nindent 12 }}
          {{- end }}
          {{- if .Values.customLivenessProbe }}
          livenessProbe: {{- include "common.tplvalues.render" (dict "value" .Values.customLivenessProbe "context" $) | nindent 12 }}
          {{- else if .Values.livenessProbe.enabled }}
          livenessProbe: {{- include "common.tplvalues.render" (dict "value" (omit .Values.livenessProbe "enabled") "context" $) | nindent 12 }}
          {{- end }}
          {{- if .Values.customReadinessProbe }}
          readinessProbe: {{- include "common.tplvalues.render" (dict "value" .Values.customReadinessProbe "context" $) | nindent 12 }}
          {{- else if .Values.readinessProbe.enabled }}
          readinessProbe: {{- include "common.tplvalues.render" (dict "value" (omit .Values.readinessProbe "enabled") "context" $) | nindent 12 }}
          {{- end }}
          {{- if .Values.resources }}
          resources: {{- toYaml .Values.resources | nindent 12 }}
          {{- else if ne .Values.resourcesPreset "none" }}
          resources: {{- include "common.resources.preset" (dict "type" .Values.resourcesPreset) | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: empty-dir
              mountPath: /etc/apache2/sites-enabled
              subPath: apache-conf-dir
            - name: empty-dir
              mountPath: /var/log/apache2
              subPath: apache-logs-dir
            - name: empty-dir
              mountPath: /var/run/apache2
              subPath: apache-tmp-dir
            - name: empty-dir
              mountPath: /usr/local/etc/php/conf.d
              subPath: php-conf-dir
            - name: empty-dir
              mountPath: /tmp
              subPath: tmp-dir
            - name: mediawiki-data
              mountPath: /var/www/html
              subPath: mediawiki
            - name: apache-config
              mountPath: /etc/apache2/ports.conf
              subPath: ports.conf
            - name: custom-settings
              mountPath: /config
            {{- if .Values.extraVolumeMounts }}
            {{- include "common.tplvalues.render" (dict "value" .Values.extraVolumeMounts "context" $) | nindent 12 }}
            {{- end }}
        {{- if .Values.metrics.enabled }}
        - name: metrics
          image: {{ template "mediawiki.metrics.image" . }}
          imagePullPolicy: {{ .Values.metrics.image.pullPolicy | quote }}
          command: [ '/bin/apache_exporter', '--scrape_uri', 'http://status.localhost:8080/server-status/?auto']
          ports:
            - name: metrics
              containerPort: 9117
          livenessProbe:
            httpGet:
              path: /metrics
              port: metrics
            initialDelaySeconds: 15
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /metrics
              port: metrics
            initialDelaySeconds: 5
            timeoutSeconds: 1
          {{- if .Values.metrics.resources }}
          resources: {{- toYaml .Values.metrics.resources | nindent 12 }}
          {{- else if ne .Values.metrics.resourcesPreset "none" }}
          resources: {{- include "common.resources.preset" (dict "type" .Values.metrics.resourcesPreset) | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: empty-dir
              mountPath: /tmp
              subPath: tmp-dir
        {{- end }}
        {{- if .Values.sidecars }}
        {{- include "common.tplvalues.render" ( dict "value" .Values.sidecars "context" $) | nindent 8 }}
        {{- end }}
      volumes:
        - name: empty-dir
          emptyDir: {}
        - name: mediawiki-data
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ if .Values.persistence.existingClaim }}{{ .Values.persistence.existingClaim }}{{- else }}{{ template "common.names.fullname" . }}-mediawiki{{- end }}
          {{- else }}
          emptyDir: {}
          {{- end }}
        - name: apache-config
          configMap:
            name: {{ template "common.names.fullname" . }}-apache-config
        - name: custom-settings
          configMap:
            name: {{ template "common.names.fullname" . }}-localsettings-custom
        {{- if .Values.extraVolumes }}
        {{- include "common.tplvalues.render" (dict "value" .Values.extraVolumes "context" $) | nindent 8 }}
        {{- end }}
{{- end }}
