{{- if .Values.mariadb.enabled }}
{{- /*
Copyright Broadcom, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and .Values.mariadb.tls.enabled .Values.mariadb.tls.autoGenerated.enabled (eq .Values.mariadb.tls.autoGenerated.engine "cert-manager") }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ printf "%s-crt" (include "common.names.fullname" .) }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.mariadb.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/part-of: mariadb
  {{- if .Values.mariadb.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.mariadb.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  secretName: {{ template "mariadb.tls.secretName" . }}
  commonName: {{ printf "%s.%s.svc.%s" (include "common.names.fullname" .) (include "common.names.namespace" .) .Values.mariadb.clusterDomain }}
  issuerRef:
    name: {{ printf "%s-ca-issuer" (include "common.names.fullname" .) }}
    kind: Issuer
  subject:
    organizations:
      - "MariaDB"
  dnsNames:
  {{- $headlessServiceName := printf "%s-headless" (include "common.names.fullname" .) | trunc 63 | trimSuffix "-" }}
  {{- $dnsNames := include "common.certs.sans" (dict "namespace" (include "common.names.namespace" .) "clusterDomain" .Values.mariadb.clusterDomain "serviceName" (include "mariadb.primary.fullname" .) "headlessServiceName" $headlessServiceName "loopback" .Values.mariadb.tls.autoGenerated.loopback "extraSANs" (.Values.mariadb.tls.autoGenerated.extraSANs | default (list))) | splitList " " | uniq }}
  {{- $dnsNames = include "common.certs.sans" (dict "namespace" (include "common.names.namespace" .) "clusterDomain" .Values.mariadb.clusterDomain "serviceName" (include "mariadb.secondary.fullname" .)) | splitList " " | concat $dnsNames | uniq }}
    {{- range $dnsNames }}
    - {{ . | quote }}
    {{- end }}
  {{- if .Values.mariadb.tls.autoGenerated.loopback }}
  ipAddresses:
    - 127.0.0.1
  {{- end }}
  privateKey:
    algorithm: {{ .Values.mariadb.tls.autoGenerated.certManager.keyAlgorithm }}
    size: {{ int .Values.mariadb.tls.autoGenerated.certManager.keySize }}
  duration: {{ .Values.mariadb.tls.autoGenerated.certManager.duration }}
  renewBefore: {{ .Values.mariadb.tls.autoGenerated.certManager.renewBefore }}
{{- end }}
{{- end }}
