{{- if .Values.mariadb.enabled }}
{{- /*
Copyright Broadcom, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and .Values.mariadb.passwordUpdateJob.enabled (eq ( include "mariadb.createPreviousSecret" . ) "true") }}
{{- $rootPassword := .Values.mariadb.passwordUpdateJob.previousPasswords.rootPassword }}
{{- $password := .Values.mariadb.passwordUpdateJob.previousPasswords.password }}
{{- $replicationPassword := .Values.mariadb.passwordUpdateJob.previousPasswords.replicationPassword }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ printf "%s-previous-secret" (include "common.names.fullname" .) | trunc 63 | trimSuffix "-" }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.mariadb.commonLabels "context" $ ) | nindent 4 }}
  {{- $defaultAnnotations := dict "helm.sh/hook" "pre-upgrade" "helm.sh/hook-delete-policy" "hook-succeeded" }}
  {{- $annotations := include "common.tplvalues.merge" ( dict "values" ( list .Values.mariadb.commonAnnotations $defaultAnnotations ) "context" . ) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $ ) | nindent 4 }}
type: Opaque
data:
  mariadb-root-password: {{ required "The previous root password is required!" $rootPassword | b64enc | quote }}
  {{- if .Values.mariadb.auth.username }}
  mariadb-password: {{ required "The previous user password is required!" $password | b64enc | quote }}
  {{- end }}
  {{- if eq .Values.mariadb.architecture "replication" }}
  mariadb-replication-password: {{ required "The previous replication password is required!" $replicationPassword | b64enc | quote }}
  {{- end }}
{{- end }}
{{- end }}
